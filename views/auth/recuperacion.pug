extends ../layout/admin-auth

block contenido

    if errores 
        div(class="max-w-md mx-auto my-10")
            each error in errores
                p(class="bg-red-600 text-white uppercase text-xs text-center p-2 mb-1 font-bold")=error.msg 

    div(class="min-h-screen flex items-center justify-center bg-gray-100")
        div(class="bg-white p-8 rounded-lg shadow-md w-96 ")
            h2(class="text-2xl font-bold mb-6 text-center") Recupera tu Contraseña
            form(action="/auth/login", method="POST", class="space-y-4" noValidate)
                input(type="hidden" name="_csrf" value= csrfToken)
                div(class="space-y-2")
                label(for="email", class="block text-sm font-medium text-gray-700") Email
                input#email(
                    type="email"
                    name="email"
                    class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500"
                )
                
                button(
                type="submit"
                class="w-full bg-green-400 text-white py-2 px-4 rounded-md hover:bg-green-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
                ) Enviar Correo