extends ./layout/admin

block contenido

    div(class="mx-auto")
        div(class="relative w-full overflow-hidden")
            div(id="carousel" class="flex transition-transform duration-700 ease-in-out" style="transform: translateX(0%);")
                div(class="min-w-full")
                    img(src="https://via.placeholder.com/800x400?text=Imagen+1" alt="Imagen 1" class="w-full")
                div( class="min-w-full")
                    img(src="https://via.placeholder.com/800x400?text=Imagen+2" alt="Imagen 2" class="w-full")
                div(class="min-w-full")
                    img(src="https://via.placeholder.com/800x400?text=Imagen+3" alt="Imagen 3" class="w-full")
            button(id="prev" class="absolute top-1/2 left-0 transform -translate-y-1/2 px-4 py-2 bg-gray-800 text-white opacity-75 hover:opacity-100 transition-opacity")
                svg(xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-arrow-left h-4 w-4")
                    path( d="m12 19-7-7 7-7")
                    path( d="M19 12H5")
            button(id="next" class="absolute top-1/2 right-0 transform -translate-y-1/2 px-4 py-2 bg-gray-800 text-white opacity-75 hover:opacity-100 transition-opacity")
                svg(xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-arrow-right h-4 w-4")
                    path(d="M5 12h14")
                    path(d="m12 5 7 7-7 7")
    script.
        const carousel = document.getElementById('carousel');
        const prevButton = document.getElementById('prev');
        const nextButton = document.getElementById('next');
        const totalItems = carousel.children.length;
        let currentIndex = 0;
        const intervalTime = 4000;

        prevButton.addEventListener('click', () => {
            currentIndex = (currentIndex > 0) ? currentIndex - 1 : totalItems - 1;
            updateCarousel();
        });

        nextButton.addEventListener('click', () => {
            currentIndex = (currentIndex < totalItems - 1) ? currentIndex + 1 : 0;
            updateCarousel();
        });

        function updateCarousel() {
            carousel.style.transform = `translateX(-${currentIndex * 100}%)`;
        }
        // Configurar el auto-desplazamiento
        function autoSlide() {
            currentIndex = (currentIndex < totalItems - 1) ? currentIndex + 1 : 0;
            updateCarousel();
        }

        // Intervalo para el auto-desplazamiento
        let autoSlideInterval = setInterval(autoSlide, intervalTime);

        // Reiniciar el auto-desplazamiento al interactuar con los botones
        function resetAutoSlide() {
            clearInterval(autoSlideInterval);
            autoSlideInterval = setInterval(autoSlide, intervalTime);
        }

    div(class="px-4 py-6 md:px-20 md:py-12 grid grid-cols-2 gap-4 bg-green-400")
        a(href="#")
            div(class="relative h-[200px] md:h-[300px] overflow-hidden rounded-lg")
                img(src="./img/bazar.jpg" alt="Bazar" width="400" height="300" style="aspect-ratio: 400 / 300; object-fit: cover;" class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-110")
                div(class="absolute inset-0 bg-black/50 flex items-center justify-center transition-opacity duration-300 group-hover:opacity-0")
                    h3(class="text-white text-2xl font-bold uppercase") Bazar
        a(href="#")
            div(class="relative h-[200px] md:h-[300px] overflow-hidden rounded-lg")
                img(src="./img/cocina.jpg" alt="Cocina" width="400" height="300" style="aspect-ratio: 400 / 300; object-fit: cover;" class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-110")
                div(class="absolute inset-0 bg-black/50 flex items-center justify-center transition-opacity duration-300 group-hover:opacity-0")
                    h3(class="text-white text-2xl font-bold uppercase") Cocina 
        a(href="#")
            div(class="relative h-[200px] md:h-[300px] overflow-hidden rounded-lg")
                img(src="./img/dormitorio.jpg" alt="Dormitorio" width="400" height="300" style="aspect-ratio: 400 / 300; object-fit: cover;" class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-110")
                div(class="absolute inset-0 bg-black/50 flex items-center justify-center transition-opacity duration-300 group-hover:opacity-0")
                    h3(class="text-white text-2xl font-bold uppercase") Dormitorio
        a(href="#")
            div(class="relative h-[200px] md:h-[300px] overflow-hidden rounded-lg")
                img(src="./img/bano.jpeg" alt="Baño" width="400" height="300" style="aspect-ratio: 400 / 300; object-fit: cover;" class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-110")
                div(class="absolute inset-0 bg-black/50 flex items-center justify-center transition-opacity duration-300 group-hover:opacity-0")
                    h3(class="text-white text-2xl font-bold uppercase") Baño
    div(class="")
        H2(class="uppercase font-bold text-2xl text-center p-2") Ultimos Ingresos
        div(class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 p-4 md:p-6 md:px-20")
            include ./templates/card